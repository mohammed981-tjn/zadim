# زاديم — تعليمات دائمة

متجرٌ إلكترونيٌّ سعوديّ: **Medusa v2.19 + Next.js 16 + PostgreSQL 16**،
عربيٌّ RTL وإنجليزيٌّ LTR. والواجهةُ **صفرُ منطقِ عمل** بقرارٍ معماريّ.

## اقرأ هذا أوّلاً في كل جلسة

1. **`docs/roadmap-priority.md` — الخطّة المرتَّبة بالأولوية.** ماذا يُبنى
   تالياً ولماذا وبأيّ شرطِ قبول. **ابدأ من أعلى القائمة ولا تقفز.**
2. **`docs/audit/production-readiness.md`** — جدولُ «مكتمل/جزئيّ/لا»
   لكلّ مجال، وما ينقص كلَّ بندٍ بالضبط.
3. **`docs/16-behavior-map.md`** — ما يفعله النظامُ **فعلاً**: الحرّاسُ
   في القاعدة، معادلاتُ المال وثوابتُها، وما لا تحرسه البوّابات.
   **يُقرأ قبل أيّ دفعة**: أكثرُ ما ضاع من وقتٍ هنا كان بناءَ حلٍّ
   لمشكلةٍ غير قائمة، أو إعادةَ بناء ما هو مبنيّ.
4. **`docs/audit/documentation-drift.md`** — الوثائقُ تتقادم في هذا
   المستودع. **الكودُ + الاختبارُ + المسارُ الإنتاجيّ هو مصدرُ الحقيقة**،
   لا الوثيقة. وإن وجدتَ تعارضاً: صحّحِ الوثيقةَ وسجّلْه هنا.

---

## 🔴 القاعدة الحاكمة: ما يُعدّ «مكتملاً»

**لا يُعدّ بندٌ منفَّذاً لأن الكودَ مكتوب.** يُعدّ منفَّذاً حين:

1. له **مسارُ إنتاجٍ حقيقيّ** — لا دالّةٌ تناديها البوّابةُ وحدَها.
2. **الواجهةُ تصله** حين يلزم.
3. **الصلاحياتُ تمنع** غيرَ المسموح.
4. **القاعدةُ تحرس** الثابتَ المهمّ — لا شرطُ `if` في خدمة.
5. له **شاهدٌ موجبٌ وشاهدٌ سالب**.
6. وإن كان حسّاساً للتزامن: **اختبارُ تزامنٍ حقيقيّ**.
7. ويعمل على **Postgres حقيقية** لا محاكاة.

وقد كلّف تجاهلُ هذا ثمناً مقيساً: فحصٌ في 2026-09-03 وجد **ثلاث وحداتٍ
مكتملةً مُختبَرةً لا يناديها مسارُ إنتاجٍ واحد** — الفوترةُ وحدُّ الدفع
عند الاستلام وتصريفُ التسويق. وبوّاباتُها كلُّها كانت **خضراء**، لأنها
تنادي الدالّةَ بيدها. فكان المتجرُ يبيع **بصفر فاتورة**.

**والحارسُ ضدّ ذلك مبنيٌّ**: `verify-wiring.ts` تسأل «هل يناديها أحدٌ
خارج `src/scripts/`؟». **أضفْ إليها كلَّ قدرةٍ حرجةٍ تبنيها.**

---

## أخطاءٌ وقعت فعلاً — لا تكرّرها

| الخطأ | ما حدث | القاعدة |
|---|---|---|
| **الاكتفاء بالبوّابات قبل الدفع** | ٢٠ بوّابةً خضراءَ محليّاً، وسقط CI في **البناء** — `medusa exec` يترجم ما يحتاجه، و`medusa build` يُشغّل `tsc` كاملاً | **شغّلْ `bash scripts/preflight.sh` قبل كل دفع** |
| **بذرةٌ «مريحة»** | منتجٌ بـ٩٩٫٩٩ ريالاً **لم يكن يُشترى إطلاقاً** (حلقةُ `PRICE_CHANGED`)، وكلُّ البوّابات خضراء لأن سعرَي البذرة من مضاعفات العشرين **صدفةً** | اختر أرقاماً **غيرَ مريحة**: ٩٩٫٩٩ · ١٩٫٩٩ · ٧٫١٣ |
| **إعادةُ بناء ما هو مبنيّ** | Medusa يملك `is_automatic` وقواعدَ العروض والحدَّ الكلّيَّ بقفلِ صفّ | **اقرأ `node_modules/@medusajs/*` قبل أن تبني** — وابنِ ما ينقص فقط |
| **الوثوقُ بجدول التغطية** | صفٌّ قال «التقييماتُ لم تدخل» وهي داخلةٌ منذ بُنيت | **قِسْ بالكود لا بالوثيقة** |
| **فحصٌ يقفز فوق الحارس** | بوّابةٌ نادت الإتمامَ بلا عرضٍ سابق، فقفزت فوق `if (quote && …)` ومرّت **خضراءَ على العطب** | **انقضِ الفحصَ**: أزلِ الإصلاحَ وتأكّدْ أنه يسقط |
| **عدُّ الملفّات بدل قراءة التركيب** | ادّعيتُ «لا شاشةَ للمفضّلة» وهي مركَّبةٌ في صفحة الحساب | اقرأ ما تركّبه الصفحةُ لا أسماءَ الملفّات |
| **`pkill -f` واسع** | قتل تشغيلتي الجديدةَ مع القديمة | لا أنماطَ قتلٍ عامّة |

---

## قواعدُ لا تُكسر

- **المالُ بالهللات صحيحةً** (ADR-008). ولا `FLOAT` في أيّ حقلٍ ماليّ.
  والتقريبُ عند **معبرٍ واحد**: `amount()` في `checkout/pricing.ts`
  (ADR-034). ولا تُقرّبْ في مكانٍ آخر.
- **توازنُ المجاميع يُفحص على أرقام Medusa الخام** لا على المقرَّب —
  وإلا مُنع بيعٌ مشروعٌ بهللةٍ (١٢ تركيبةً من ١٠٥ مقيسة).
- **لا قاعدةَ عملٍ مبرمَجة** (بند ٤٨): الأسعارُ والضرائبُ والشحنُ
  والحدودُ والمهلُ **صفوفٌ** يضبطها المدير. وفاحصٌ في CI يمنع المخالفة.
- **الدفاترُ تُلحَق ولا تُمسّ**: سجلُّ التدقيق · حركاتُ المخزون ·
  إيصالاتُ الشراء · استهلاكُ الكوبونات. التصحيحُ **بصفٍّ مقابل**.
- **الرفضُ قبل أخذ المال**، ويُقاس **بعدّ الطلبات قبل الرفض وبعده** —
  رفضٌ يُعيد رسالةً ويُنشئ الطلبَ خلفه أسوأُ من قبولٍ صريح.
- **لا `customer_id` من جسم الطلب أبداً** — يُشتقّ من رمز الجلسة
  (`modules/checkout/identity.ts`).
- **التعليقاتُ بالعربية وتشرح «لماذا» لا «ماذا»**: القرارَ الهندسيَّ
  وبديلَه المرفوضَ وسببَه. طابقْ كثافةَ التعليق في الملفّ الذي تعدّله.

---

## كيف تشتغل

```bash
# قاعدةُ التطوير (مُهيّأةٌ ومبذورة)
su postgres -c "/usr/lib/postgresql/16/bin/pg_ctl -D /var/lib/postgresql/zadimtest -l /tmp/pg.log start"

export DATABASE_URL="postgres://zadim:zadim@localhost:5432/zadim_test"
export DATABASE_SCHEMA=zadim JWT_SECRET=x COOKIE_SECRET=x

# بوّابةٌ واحدة
cd apps/backend && npx medusa exec ./src/scripts/verify-<اسم>.ts

# 🔴 قبل كلّ دفع — إلزاميّ
bash scripts/preflight.sh            # بناءٌ + أنواعُ الواجهة + الأدلّة
bash scripts/preflight.sh --gates    # ومعها البوّابات كلُّها
```

**والفرعُ والدفعُ**: العملُ على `claude/zadim-repository-audit-aprfbe`،
وطلبُ الدمج **مسودّةً**، ولا يُدمج إلا بخضرة `verify` و`verify-ui`
وCodeQL.

---

## ما ينتظر المالك — ولا يُنجَز بلا مفاتيحه

هذه **ليست فجواتٍ برمجية**. لا تحاول الالتفافَ عليها:

1. **النشرُ على Railway** — الخلفيّةُ لم تُنشر قطّ، والواجهةُ على Vercel
   تعرض «تعذّر التحميل» لكلّ زائر. انظر `docs/10-deployment.md`.
2. **مزوّدُ دفعٍ حقيقيّ** أو قرارٌ صريحٌ بـ«COD وحدَه».
3. **إعداداتُ ZATCA** (اسمُ البائع ورقمُه الضريبيّ) — وبدونها يبيع
   المتجرُ **بلا فاتورة**، **والفائتُ لا يدخل السلسلةَ بأثرٍ رجعيّ**.
4. **حسابُ رسائل** — الإشعاراتُ تقف عند `queued`، واستعادةُ كلمة المرور
   معطَّلةٌ بلا مزوّد.
5. **حسابُ ناقلٍ** للبوليصة والتتبّع.
