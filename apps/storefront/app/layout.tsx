import "./globals.css"

/**
 * الجذرُ **بلا لغةٍ ولا اتجاه** — يضبطهما `app/[locale]/layout.tsx`.
 *
 * ⚠️ ولماذا يبقى هذا الملفُّ أصلاً: Next يشترط تخطيطاً جذرياً فيه
 * `<html>` و`<body>`. ولو وُضع `lang="ar" dir="rtl"` هنا لصار كلُّ
 * صفحةٍ إنجليزيةٍ **عربيةَ الاتجاه في مصدرها** ثم تُصحَّح في الطبقة
 * التالية — أو لا تُصحَّح ويقرأ القارئُ الآليُّ العربيةَ إنجليزية.
 *
 * فيبقى الجذرُ صامتاً عن اللغة، وتقولها الطبقةُ التي تعرفها.
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
