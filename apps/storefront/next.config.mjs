/**
 * ⚠️ **لا `ignoreBuildErrors` هنا.**
 *
 * وصلت هذه الواجهةُ من v0 وفيها `typescript: { ignoreBuildErrors: true }`،
 * وتحتها خطآ نوعٍ حقيقيّان في `app/checkout/page.tsx` **مرّا في البناء**.
 * وبناءٌ لا يستطيع أن يفشل ليس بناءً بل ختمُ موافقة: يخضرّ على كودٍ لا
 * يُترجم، فيُقرأ خضارُه أماناً وهو صمت.
 *
 * فحُذف السطر، وأُصلح الخطآن. وأيُّ خطأ نوعٍ بعد اليوم **يوقف البناء**.
 *
 * ── و`images.unoptimized` يبقى ────────────────────────────────────
 *
 * صورُ المنتجات تأتي بعناوينَ من الخلفيّة (`thumbnail`)، ومحسِّنُ Next
 * يحتاج قائمةَ نطاقاتٍ مسموحة تُضبط وقتَ البناء — وهي بيانات يضيفها
 * المديرُ لا كودٌ يُنشر. فالتحسينُ مُطفأ حتى تُحسم مسألةُ استضافة الصور
 * (المرحلة ١٦)، ولا تسقط صورةٌ لأن نطاقَها لم يُدرَج في ملفّ.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
}

export default nextConfig
