/**
 * ⚠️ **لا `ignoreBuildErrors` هنا.**
 *
 * وصلت هذه الواجهةُ من v0 وفيها `typescript: { ignoreBuildErrors: true }`،
 * وتحتها خطآ نوعٍ حقيقيّان في `app/checkout/page.tsx` **مرّا في البناء**.
 * وبناءٌ لا يستطيع أن يفشل ليس بناءً بل ختمُ موافقة: يخضرّ على كودٍ لا
 * يُترجم، فيُقرأ خضارُه أماناً وهو صمت.
 *
 * فحُذف السطر، وأُصلح الخطآن. وأيُّ خطأ نوعٍ بعد اليوم **يوقف البناء**.
 *
 * ── و`images.unoptimized` يبقى ────────────────────────────────────
 *
 * صورُ المنتجات تأتي بعناوينَ من الخلفيّة (`thumbnail`)، ومحسِّنُ Next
 * يحتاج قائمةَ نطاقاتٍ مسموحة تُضبط وقتَ البناء — وهي بيانات يضيفها
 * المديرُ لا كودٌ يُنشر. فالتحسينُ مُطفأ حتى تُحسم مسألةُ استضافة الصور
 * (المرحلة ١٦)، ولا تسقط صورةٌ لأن نطاقَها لم يُدرَج في ملفّ.
 */

import { dirname } from "node:path"
import { fileURLToPath } from "node:url"

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },

  /**
   * ⚠️ **جذرُ مساحة العمل يُصرَّح به، ولا يُستنتج.**
   *
   * في المستودع قفلان: `apps/storefront/package-lock.json` وقفلٌ في
   * الجذر (أُضيف في المرحلة ٩ لأدوات فحص الواجهة — Playwright
   * وLighthouse). فيرى Next قفلين ويحذّر أنه **خمّن** الجذر:
   * «We detected multiple lockfiles».
   *
   * وهو تحذيرٌ لا خطأ — البناءُ ينجح. لكنّ الجذرَ يقرّر ما تتعقّبه
   * حزمةُ الإخراج، وتخمينُه الخطأ يعني ملفّاتٍ ناقصةً في نشرةٍ بُنيت
   * خضراء. فيُحسم صراحةً بمجلَّد هذا الملفّ.
   */
  turbopack: {
    root: dirname(fileURLToPath(import.meta.url)),
  },

  /**
   * 🔴 **روابطُ الأصول الرقمية بإعادةِ كتابةٍ لا بمجلَّدٍ مخفيّ.**
   *
   * Chrome يطلب `/.well-known/assetlinks.json` بهذا الاسم حرفاً بحرف.
   * وبناءُ المسار في `app/.well-known/…` يُخرج مجلَّداً **يبدأ بنقطة**،
   * والمجلَّداتُ المخفيّةُ تُتخطّى في حزمِ النشر — فتُشير خريطةُ التوجيه
   * إلى دالّةٍ لم تُرفع، **وتسقط النشرةُ كلُّها إلى ٤٠٤** لا ذلك المسارُ
   * وحدَه. والبناءُ يخضرّ ولا شيءَ يقول إن الموقعَ لا يُخدَم.
   *
   * فالعنوانُ العامُّ يبقى كما يطلبه Chrome، والدالّةُ تسكن `/api/`.
   *
   * و`beforeFiles` لأنّ الوجهةَ مسارٌ حقيقيّ: لو كانت `afterFiles`
   * لجُرِّب نظامُ الملفّات أوّلاً ثمّ أُعيدت الكتابة — والنتيجةُ نفسُها
   * هنا، لكنّ `beforeFiles` تقول النيّةَ صراحة: هذا العنوانُ اسمٌ مستعار.
   */
  async rewrites() {
    return {
      beforeFiles: [
        { source: "/.well-known/assetlinks.json", destination: "/api/assetlinks" },
      ],
    }
  },
}

export default nextConfig
