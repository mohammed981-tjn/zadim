import { loadEnv, defineConfig, Modules } from "@medusajs/framework/utils";
import { discoverCarriers } from "./src/modules/carriers/discover";

loadEnv(process.env.NODE_ENV || "development", process.cwd());

/**
 * إعداد خادم التجارة (ADR-001: Medusa v2 أساساً، ووحداتُنا فوقه).
 *
 * القاعدة هنا: **لا سرَّ في هذا الملف**. كلُّ مفتاحٍ يُقرأ من البيئة،
 * والملفّ يُدفع إلى مستودعٍ عامّ. وما لا قيمةَ افتراضيةَ آمنةً له
 * (JWT_SECRET · COOKIE_SECRET) **يُسقط الإقلاع في الإنتاج** بدل أن
 * يعمل بقيمةٍ معروفةٍ للجميع — الفشلُ الصاخب أأمنُ من التشغيل الصامت.
 */

const isProduction = process.env.NODE_ENV === "production";

function requiredInProduction(name: string, devFallback: string): string {
  const value = process.env[name];
  if (value) return value;
  if (isProduction) {
    throw new Error(
      `[zadim] ${name} غير مضبوط. لا يُقلع الخادم في الإنتاج بسرٍّ افتراضي.`
    );
  }
  return devFallback;
}

export default defineConfig({
  projectConfig: {
    databaseUrl: process.env.DATABASE_URL,

    // 🔴 مخطَّطٌ خاصٌّ بنا لا `public` (ADR-009، وباقٍ بعد ADR-033).
    //
    // القاعدةُ الآن في Railway مع الخادم لا في مشروعٍ مشترك — والمخطَّطُ
    // المنفصل يبقى: قاعدةٌ خاصّةٌ اليوم قد تستضيف غيرَنا غداً، والعزلُ
    // أرخصُ من الفرز في قاعدةٍ حيّة.
    //
    // ⚠️ **وهذا السطرُ وحدَه لا يكفي.** `medusa db:migrate` يضع جداولَ
    // وحداتنا هنا، وهجراتُ Medusa الأساسية تكتب SQL خاماً بأسماءٍ غيرِ
    // مؤهَّلة فتذهب حيث يقول `search_path` — وافتراضُه `"$user", public`.
    // فيلزم على القاعدة **مرّةً واحدة**:
    //
    //   alter role <الدور> in database <القاعدة> set search_path = zadim, public;
    //
    // ويرفض `src/scripts/migrate-once.ts` الهجرةَ إن لم يكن مضبوطاً
    // (ADR-033: «كان يعمل بالصدفة لا بالإعداد»).
    databaseSchema: process.env.DATABASE_SCHEMA || "zadim",
    redisUrl: process.env.REDIS_URL,
    http: {
      storeCors: process.env.STORE_CORS || "http://localhost:8000",
      adminCors: process.env.ADMIN_CORS || "http://localhost:9000",
      authCors: process.env.AUTH_CORS || "http://localhost:9000",
      jwtSecret: requiredInProduction("JWT_SECRET", "dev-only-jwt-secret"),
      cookieSecret: requiredInProduction("COOKIE_SECRET", "dev-only-cookie-secret"),
    },
  },

  admin: {
    // لوحة Medusa الإدارية. مركزُ القيادة الكامل (بند ٢٩) يُبنى في
    // المرحلة ٨؛ وهذه تكفي لإدارة الكتالوج والطلبات حتى ذلك الحين.
    //
    // ويُعطَّل عرضُها بمفتاحِ بيئة لأن **نُسَخ العامل** (التي تشغّل
    // الطوابير والمهامّ المجدولة) لا تخدم واجهةً أصلاً، ومحاولتُها
    // تحميلَ حزمةِ اللوحة تُسقط إقلاعَها. والافتراضُ مفعَّل.
    disable: process.env.MEDUSA_ADMIN_DISABLED === "true",
  },

  modules: [
    // ── الملفّات: مزوّدٌ **مضبوطٌ صراحةً** لا افتراضيّ ──────────────
    //
    // 🔴 والسبب أخطرُ ما قِيس في تجهيز النشر: مزوّدُ Medusa الافتراضيّ
    // يستعمل `backend_url = "http://localhost:9000/static"`. فأوّلُ
    // صورةٍ تُرفع في الإنتاج **تُكتب في القاعدة برابط localhost** لا
    // يفتحه زائرٌ أبداً. ولا خطأ، ولا تحذير: الرفعُ ينجح، واللوحةُ
    // تعرض الصورة (لأنها على جهاز الرافع)، والزائرُ يرى فراغاً.
    //
    // وأسوأُ ما فيه أنه **يُفسد البيانات لا العرض**: الرابطُ الخاطئ
    // مخزَّنٌ في صفوف المنتجات، ويبقى خاطئاً بعد إصلاح الإعداد.
    // فتصحيحُه لاحقاً هجرةُ بياناتٍ لا تغييرُ سطر.
    //
    // فيُطلب `MEDUSA_BACKEND_URL` في الإنتاج ويسقط الإقلاع بدونه —
    // نفسُ قاعدة `JWT_SECRET`: الفشلُ الصاخب أأمنُ من التشغيل الصامت.
    //
    // ⚠️ **والقرصُ يجب أن يكون دائماً.** `static/` تحت مجلّد العمل،
    // وحاويةٌ بلا قرصٍ مثبَّت تفقد كلَّ الصور مع كل نشرة. (وحين يكبر
    // المتجر ينتقل هذا إلى S3 بتبديل المزوّد وحدَه — والمُنادي لا
    // يتغيّر، لأن الرفعَ يمرّ بـ`Modules.FILE` لا بالقرص مباشرةً.)
    {
      resolve: "@medusajs/medusa/file",
      options: {
        providers: [
          {
            resolve: "@medusajs/medusa/file-local",
            id: "local",
            options: {
              upload_dir: process.env.UPLOAD_DIR || "static",
              backend_url: `${requiredInProduction(
                "MEDUSA_BACKEND_URL",
                "http://localhost:9000"
              ).replace(/\/+$/, "")}/static`,
            },
          },
        ],
      },
    },

    // ── وحداتُنا نحن ────────────────────────────────────────────────
    // ما لا يقدّمه Medusa ولا مفرّ من كتابته (00-decisions.md §ADR-001):
    // access (RBAC + سجلّ التدقيق) · wms · purchasing · loyalty · cms ·
    // finance/ZATCA · marketplace.
    //
    // المرحلة ١ تبدأ بـ access وحدها — بند ٤٥ وبند ٤٦.
    { resolve: "./src/modules/access" },

    // المرحلة ٢: الخصائص والفلاتر المتولّدة وتطبيعُ العربية ومرادفاتُ
    // البحث — ما لا تقدّمه وحدةُ منتجات Medusa.
    { resolve: "./src/modules/catalog" },

    // ثوابتُ القاعدة التي تعبر حدود الوحدات — أهمُّها منعُ البيع الزائد
    // بقيدٍ على `inventory_level` (جدولُ Medusa، وفيه صفرُ قيودِ فحصٍ
    // أصلاً). انظر migrations/Migration20260901000001.ts.
    { resolve: "./src/modules/integrity" },

    // المرحلة ٣: ما لا تحفظه وحدةُ مخزون Medusa — ملفُّ المستودع الذي
    // يقرّر «من أين يُشحن»، ودفترُ الحركات الذي يجيب «من أين نقصت»،
    // وحدُّ تنبيه النفاد بياناتٍ لا رقماً في الكود.
    { resolve: "./src/modules/warehouse" },

    // المرحلة ٤: ما تفتقده سلّةُ Medusa — لحظةُ العرض التي يُقاس منها
    // «تغيّر السعر»، وحارسُ التكرار الذي يمنع طلبين من ضغطتين.
    { resolve: "./src/modules/checkout" },

    // المرحلة ٥: آلةُ حالات الطلب جدولَ بياناتٍ يقرؤه مُطلِقٌ في القاعدة،
    // وصندوقُ أحداثٍ يُكتب في نفس معاملة التغيّر، وحرمةُ الفاتورة.
    { resolve: "./src/modules/orders" },

    // ── الشحن ───────────────────────────────────────────────────────
    // 🔴 **لا اسمَ ناقلٍ هنا.** المحوّلاتُ تُقرأ من مجلَّدها
    // (`src/modules/carriers/`)، فإضافةُ ناقلٍ ثانٍ **لا تعدّل هذا
    // الملفّ ولا غيرَه** — وهو نصُّ بوّابة المرحلة ٧. وسطرٌ واحدٌ باسمٍ
    // هنا يكفي ليصير الوعدُ كذبةً صغيرة، ثم تكبر.
    {
      resolve: "@medusajs/medusa/fulfillment",
      options: {
        providers: [
          // `manual_manual` يبقى: تستعمله البذرةُ والبوّابات، وهو
          // مزوّدُ Medusa الافتراضيّ لا ناقلٌ من عندنا.
          { resolve: "@medusajs/medusa/fulfillment-manual", id: "manual" },
          ...discoverCarriers(),
        ],
      },
    },

    // ── الدفع ───────────────────────────────────────────────────────
    // **الدفعُ عند الاستلام مزوّدٌ كامل الحقوق** لا استثناء
    // (`06-saudi-layer.md` §٢). وهو الوحيدُ الذي يمكن بناؤه اليوم:
    // مدى وApple Pay وتابي وتمارا كلُّها تحتاج حسابَ تاجرٍ ومفاتيحَ
    // إنتاج، **ولا تُبنى بمفتاحٍ وهميّ** (بند ٤٨).
    //
    // ⚠️ ومزوّدُ النظام (`pp_system_default`) يبقى: تستعمله بوّاباتُ
    // الفحص، وهو **ليس وسيلةَ دفعٍ للعملاء** — يُنزع يوم يصل أوّلُ
    // مزوّدٍ حقيقيّ.
    {
      resolve: "@medusajs/medusa/payment",
      options: {
        providers: [{ resolve: "./src/modules/cod-payment", id: "cod" }],
      },
    },

    // المرحلة ٦: سياسةُ COD ورفضاتُه، وحارسُ تكرار العمليات المالية.
    { resolve: "./src/modules/payments" },

    // المرحلة ٩: الرئيسيةُ كتلاً في القاعدة — ترتيبُها يتغيّر من اللوحة
    // بلا نشرِ كود، وهو نصُّ البوّابة.
    { resolve: "./src/modules/cms" },

    // المرحلة ٨: الدفعاتُ وتراجعُها — والقيمُ القديمة تُحفظ قبل الكتابة
    // لا بعدها، وإلا صار «التراجع» كتابةَ ما هو مكتوب.
    { resolve: "./src/modules/bulk" },

    // 🔴 المرحلة ١١: **التسويق من صندوق الأحداث لا من مهامٍّ تمسح
    // الجداول.** والسلّةُ المتروكة حدثٌ مؤجَّلٌ يُكتب وقتَ الفعل ويُقرأ
    // وقتَ استحقاقه — لأن «تركَ العميلُ سلّته» غيابُ فعلٍ لا فعل.
    { resolve: "./src/modules/marketing" },

    // 🔴 المرحلة ١٠: **الراجعُ يدخل الحجرَ لا الرفّ.** والحرّاسُ في
    // القاعدة على `return` و`return_item` و`inventory_level` — جداولٌ لا
    // نملكها، والطرقُ إليها كثيرة.
    { resolve: "./src/modules/returns" },

    // المرحلة ٧: اللقطُ والتغليفُ والتتبّع — ما لا تعرفه وحدةُ تنفيذ
    // Medusa: من يلقط، وبأيّ ترتيبٍ يمشي، وماذا مسح.
    { resolve: "./src/modules/fulfilment" },

    // 🔴 الفوترةُ الإلكترونية — **تُصمَّم اليوم لأن تأجيلها إعادةُ بناء**
    // (`06-saudi-layer.md` §١): سلسلةُ الفواتير لا تُضاف بأثرٍ رجعيّ.
    { resolve: "./src/modules/zatca" },

    // 🔴 التكلفةُ المجمَّدة — **من جدول «ما لا يُؤجَّل»** في
    // `07-roadmap.md`، حجّتُه «ربحُ الماضي لا يُحسب بتكلفة اليوم».
    // وكلفةُ تأخيرِه لا تُستدرَك: عمودٌ يُضاف غداً يبقى `null` لكلِّ ما
    // بيع قبله، فربحُ الأشهر الماضية لا يُحسب لا اليوم ولا أبداً.
    { resolve: "./src/modules/finance" },

    // 🔴 السوقُ — **موجودٌ ومعطَّل** (بند ٥٩)، وهو البندُ الثاني من
    // «ما لا يُؤجَّل»: `ALTER TABLE` على جدولٍ يبيع قفلٌ ينتظر خلفَه
    // كلُّ قارئ. فالأعمدةُ تُضاف اليوم والجداولُ فارغة، ولا يقرؤها
    // مسارٌ ولا يكتبها سيرُ عمل، حتى يُفتح السوق.
    { resolve: "./src/modules/marketplace" },

    // المفضّلة (بند ٢٢) — ونصُّه «**المفضّلة تعرف انخفاض السعر**».
    // فهي ليست قائمةً يفتحها العميل فحسب، بل **قائمةُ المشتركين** التي
    // كانت ناقصةً في الطرف الآخر: مُطلِقُ خفض السعر يكتب حدثاً منذ
    // المرحلة ١١، وحمولتُه بلا مستقبِل — فيُخطَّط صفرُ رسالة.
    { resolve: "./src/modules/wishlist" },

    // التقييمات (بند ٢٣) — و«**يشترط الشراء: قيدٌ لا فحصُ واجهة**»
    // (`01-domain-model.md`). والقيدُ مُطلِقٌ لا مفتاحٌ أجنبيّ، لأنه
    // يفحص **الملكيّةَ والمنتجَ** أيضاً وهما ما لا يفحصه المفتاح.
    { resolve: "./src/modules/reviews" },

    // 🔴 مزوّدُ الإشعارات (بند ٤٣) — **الطرفُ الذي يُسلِّم**.
    //
    // ولا اسمَ مزوّدٍ هنا، كما لا اسمَ ناقلٍ في وحدة الشحن: المزوّدون
    // يُقرأون من مجلَّدهم (`notify/providers/`)، وإضافةُ مزوّدِ بريدٍ
    // **مجلَّدٌ واحدٌ وكفى**.
    //
    // ومعه حارسُ إلغاء الاشتراك — ويصل معه لا بعده: رسائلُ تسويقٍ بلا
    // مخرجٍ تعني شكاوى ثم حظرَ نطاقِ المتجر، وذلك لا يُستدرَك بكود.
    { resolve: "./src/modules/notify" },

    // الموردون وأوامرُ الشراء (بندا ٣٢ و٣٣) — **والوحدةُ التي تزيد
    // المخزون**. وكلُّ ما عداها ينقصه: بيعٌ وتنفيذٌ وتلف. فبلا هذه كان
    // الرقمُ ينزل ولا يصعد إلا بتسويةٍ يدويةٍ من اللوحة — أي بلا مورّدٍ
    // ولا سعرِ شراءٍ ولا أثرٍ في الدفتر يقول «اشترينا».
    //
    // ومعها يُملأ `zadim_variant_cost` لأوّل مرّة: كان محروساً ومجمَّداً
    // **ولا كاتبَ له**، فكلُّ يومِ بيعٍ يومٌ لا يُعرف ربحُه أبداً.
    { resolve: "./src/modules/procurement" },

    // سياساتُ الكوبونات (بندا ٢٦ و٢٧) — **طبقةٌ فوق محرّك Medusa لا
    // بديلٌ عنه**. المحرّكُ يملك الرمزَ وتفرّدَه وفصلَ العرض عن الكوبون
    // والقواعدَ صفوفاً والحملاتِ والحدَّ الكلّيَّ بقفلِ صفّ — فلا يُعاد
    // بناءُ شيءٍ من ذلك.
    //
    // وهذه تحمل الثلاثةَ التي لا يملكها: **الحدُّ لكل عميل** ·
    // **سقفُ الخصم بالمال** (قِيس أن `max_quantity` سقفُ كميةٍ لا مال) ·
    // **أوّلُ طلبٍ فقط** — ومعها ترتيبُ التطبيق رقماً يضبطه المدير بدل
    // سلوكٍ ضمنيٍّ يرتّب بقيمة الخصم.
    { resolve: "./src/modules/promotions" },

    // ── البحث ───────────────────────────────────────────────────────
    // مزوّدٌ محليّ على Orama (يدعم `arabic`). و**واجهةُ المزوّد هي
    // العَتَبة** (ADR-006 المُعدَّل): الانتقالُ إلى Meilisearch يوم يضيق
    // المحليُّ يكون مزوّداً جديداً لا إعادةَ بناء.
    {
      resolve: "@medusajs/medusa/search",
      options: {
        providers: [
          { resolve: "@medusajs/medusa/search-local", id: "local" },
        ],
      },
    },
  ],
});
