# ٩) جدول التغطية — بنود المواصفة الستّون

> **الغرض**: أن يكون **مستحيلاً** أن يُختصر المشروع بصمت (بند ٤٨).
> كلُّ بندٍ من مواصفتك له صفٌّ هنا وحكمٌ صريح — و**البندُ المؤجَّل مكتوبٌ
> بسبب تأجيله ومرحلته**، لا مسكوتٌ عنه.
>
> `📐` مُصمَّم في المرحلة ٠ · `⏳` مرحلتُه محدّدة · `⚠️` مُعدَّلٌ بحجّة

| # | البند | الحالة | أين |
|---|---|---|---|
| — | **المعمار العام** | 📐 | [`01`](01-domain-model.md) · [`04`](04-api-contract.md) |
| ١ | الرئيسية ديناميكية تُرتَّب من اللوحة | ✅ **م٩** | `zadim_page_block` + `/store/home` + `block-renderer.tsx` — **جُرّب على الشاشة**: صفٌّ في القاعدة ⇒ الترتيبُ المرسوم تغيّر ([ADR-025](00-decisions.md)) |
| ٢ | محرّك بحث: مرادفات · أخطاء إملائية · عربي/إنجليزي | ✅ **م٢** | تطبيعٌ في الطرفين + مرادفاتٌ **بيانات**، ومطابقةٌ بكلمةٍ كاملةٍ أو بادئة — ودالّةٌ واحدةٌ يناديها المسارُ والفاحص ([ADR-006](00-decisions.md)) |
| ٣ | فلاتر التصنيف تتولّد من الخصائص | 🟡 **م٢** ⏳م١٠ | `GET /store/categories/:id/filters` يُعيدها بأعدادها — **ولا مسارَ يصفّي بها**، فلم تُرسم أداةُ تصفيةٍ لا تصفّي |
| ٤ | صفحة المنتج: معرض · متغيّرات · تقييمات | 🟡 **م٩** | `app/p/[handle]` — المعرضُ والمتغيّراتُ والسعرُ والتوفّر؛ **والتقييماتُ لم تدخل** |
| ٥ | **المنتج محرّكٌ لا سجلّ** | ✅ **م٢** | لا سعرَ ولا مخزونَ في `product` — قِيس على الخادم: السعرُ يأتي من `calculated_price` بمنطقةٍ، والمخزونُ من `inventory_level` |
| ٦ | السلة الذكية ومعادلة المجاميع | ✅ **م٤** | التوازنُ يُفحص في CI على **كل** طلب — و`CHECK` لا يحرسه لأن Medusa لا يخزّن المجاميع ([ADR-013](00-decisions.md)) |
| ٧ | Checkout بخطواته | ✅ **م٤ + م٩** | `orchestrate.ts` — والخطوةُ ١ لا يفعلها Medusa: السعرُ مجمَّدٌ فيُقرأ ويُقارَن ويُرفض ([ADR-013](00-decisions.md)). وشاشاتُه في `checkout-flow.tsx`، **ولوحُ `PRICE_CHANGED` رُئي مرسوماً**: الفرقُ معروضٌ والتأكيدُ مُقفلٌ حتى إعادة التسعير |
| ٨ | شراء الضيف | ✅ **م٤ + م٩** | السلّةُ والعرضُ والإتمام بـ`cart_id` وبريدٍ فقط — بلا حساب. وهويّةُ السلّة في الواجهة **ملفُّ تعريفٍ `httpOnly`** لا مخزنٌ في المتصفّح |
| ٩ | محرّك المخزون: on_hand · reserved · available · incoming | ✅ **م٣** | مُطلِقان: الحجزُ محروسٌ بـ`FOR UPDATE`، والمحجوزُ **مشتقٌّ** ([ADR-010](00-decisions.md) · [ADR-011](00-decisions.md)) — ١٠٠ متزامنة على ١٠ ⇒ **١٠** |
| ١٠ | تعدّد المستودعات واختيارُ الأقرب | ✅ **م٣** | `zadim_location_profile` + `planAllocation` — **التقسيمُ آخرُ الحلول** ([ADR-012](00-decisions.md)) |
| ١١ | آلة حالات الطلب | ✅ **م٥** ⚠️ مُعدَّل | مُطلِقٌ يقرأ `zadim_order_transition` — **المصفوفةُ كاملةً مفحوصةٌ في CI** ([ADR-016](00-decisions.md)). و«مؤكَّد» زوجٌ لا حالة: تعدادُ Medusa لا نملكه |
| ١٢ | تسلسل التنفيذ | ✅ **م٧** | `zadim_pick_list` ⇒ `zadim_parcel` ⇒ `fulfillment` — والوحدةُ شحنةٌ لا طلب |
| ١٣ | اللقط بقوائم ومسارِ مشي | ✅ **م٧** | `zadim_pick_list_item.walk_order` — فرزٌ **عدديّ** فلا يُمشى الممرُّ مرّتين |
| ١٤ | التغليف والبوليصة | 🟡 **م٧** | `zadim_parcel` بوزنٍ إلزاميّ وباركودٍ فريد؛ والبوليصةُ تنتظر ناقلاً حقيقياً |
| ١٥ | **الباركود من البداية** | ✅ **م٧** | يتحقّق ولا يثق: **مسحٌ خاطئ يوقف اللقط** ولا يُلقط بعده ([ADR-021](00-decisions.md)) |
| ١٦ | محرّك الشحن بالإدارة لا بالكود | ✅ **م٧** | فاحصٌ في CI **ومعه شاهدٌ موجب** يثبت أنه ليس أعمى ([ADR-022](00-decisions.md)) |
| ١٧ | محوّل الناقل | 🟡 **م٧** | `modules/carriers/` — يُكتشف بقراءة المجلَّد، فلا اسمَ ناقلٍ في أيّ ملفٍّ خارجه ([ADR-022](00-decisions.md)). والمحوّلاتُ الحقيقية تنتظر حساباتِ شحن |
| ١٨ | تتبّع الشحنة | 🟡 **م٧** | `zadim_shipment_event` — دفترٌ للأحداث؛ وتغذيتُه من الناقل تنتظر حسابَه |
| ١٩ | نظام المرتجعات | ✅ **م١٠** | كيانُ `return` عند Medusa + خمسةُ حرّاسَ في القاعدة: الراجعُ في الحجر، والرفُّ لا يُفتح إلا بشهادةِ فحصٍ بشريّ ([ADR-028](00-decisions.md)) |
| ٢٠ | محرّك الاسترداد بأنواعه | 🟡 **م٦ + م١٠** | مستردٌّ ≤ محصَّل، ولا تحصيلَ قبل الشحن ([ADR-019](00-decisions.md))، وسببُ الاسترداد صار مشهوداً له بسطر فحص. **ورصيدُ المتجر** بدل النقد ينتظر دفترَ القيود (م١٢) |
| ٢١ | حساب العميل بكل أقسامه | 📐 ⏳م١١ | `customers` وما يتفرّع — **لم يدخل م٩**: شراءُ الضيف يعمل، والحسابُ مؤجَّلٌ صراحةً |
| ٢٢ | المفضّلة تعرف انخفاض السعر | 🟡 **م١١** | حدثُ `PriceDropped` يُكتب في نفس معاملة تغيّر السعر؛ **والمفضّلةُ نفسُها لم تُبنَ بعد** |
| ٢٣ | التقييم يشترط شراءً | 📐 | `reviews.order_item_id` ✅ **مُختبَر** |
| ٢٤ | محرّك الولاء بشرائحه | 📐 ⏳م١٢ | `loyalty_transactions` **دفترُ قيود** |
| ٢٥ | العضوية بالاشتراك | 📐 ⏳م١٢ | `memberships` |
| ٢٦ | محرّك الكوبونات المتقدّم | 📐 ⏳م٤ | `promotions` · `promotion_rules` |
| ٢٧ | **فصلُ العرض عن الكوبون** | 📐 | `promotions.kind` — [`01`](01-domain-model.md) §3 |
| ٢٨ | التخفيض الخاطف بحدوده | 📐 ⏳م١١ | `flash_sales` بقيد `sold_within_limit` |
| ٢٩ | لوحة القيادة بالمقارنات | ✅ **م٨** | `dashboard/metrics.ts` — كلُّ رقمٍ يُحسب عند القراءة ويُقابَل بـSQL خام في CI |
| ٣٠ | إدارة المنتجات كاملةً | 📐 ⏳م٨ | |
| ٣١ | العمليات بالدفعات | ✅ **م٨** | `modules/bulk` — والتراجعُ **يتخطّى ما تغيّر بعده** ولا يدهسه ([ADR-024](00-decisions.md)) |
| ٣٢ | إدارة الموردين | 📐 ⏳م٨ | `suppliers` · `supplier_variants` |
| ٣٣ | أوامر الشراء تزيد المخزون | 📐 ⏳م٨ | `purchase_orders` · `inventory_levels.incoming` |
| ٣٤ | طبقة المالية | 📐 ⏳م١٣ | `payment_fees` · `cost` |
| ٣٥ | **هامش المساهمة الحقيقي** | 📐 ⏳م١٣ | `order_items.unit_cost` **مُجمَّدة** |
| — | **ZATCA — الفوترة الإلكترونية** | 🟡 **م٦** | السلسلةُ والتسلسلُ ورمزُ QR مبنيّةٌ ومحروسة؛ والتوقيعُ والإبلاغُ ينتظران مزوّداً معتمداً ([ADR-020](00-decisions.md)) |
| ٣٦ | محرّك الضرائب | ✅ **م٦** | منطقةٌ ضريبية بـ١٥٪ من البذرة، والضريبةُ تُحسب في السلّة وتُقيَّد في الفاتورة — والنسبةُ **بيانات** |
| ٣٧ | CMS بالكتل | ✅ **م٩** | `zadim_page_block` + `/store/home` — **جُرّب على الشاشة**: صفٌّ في القاعدة ⇒ الترتيبُ المرسوم تغيّر ([ADR-025](00-decisions.md)) |
| ٣٨ | SEO لكل كيان | ✅ **م٢** | `/store/seo/:entity/:id` · `/store/sitemap` · `/store/redirects` |
| ٣٩ | محرّك التسويق | ✅ **م١١** | أربعةُ مصادرَ من الصندوق ومن جدول التذكيرات — **وفاحصٌ في CI يمنع أيَّ ماسحِ جداول** ([ADR-029](00-decisions.md)) |
| ٤٠ | شرائح العملاء | ✅ **م١١** | `zadim_customer_segment.definition` — قواعدُ jsonb تُقيّمها دالّةٌ خالصة، **وشريحةٌ بلا قواعدَ لا تُطابِق أحداً** |
| ٤١ | الذكاء الاصطناعي | ⚠️ **مؤجَّلٌ كلُّه ⏳م١٤** | [ADR-005](00-decisions.md) — **نماذجُ بلا بياناتٍ تتعلّم منها**. ويبقى في المعمار **نقطةُ ربطٍ** عبر `outbox_events` فلا يحتاج تعديلَ المحرّك لاحقاً. وهو موضعُه في ترتيبك أصلاً |
| ٤٢ | مركز دعم العملاء | 📐 ⏳م١١ | `support_tickets` · `support_messages` |
| ٤٣ | الإشعارات مدفوعةً بالأحداث | 🟡 **م١١** | القوالبُ بلغتين والمفتاحُ يمنع الإرسالَ مرّتين؛ **والمزوّدُ يسجّل ولا يرسل** حتى يصل حسابُ رسائل |
| ٤٤ | الأمن بكل بنوده | 📐 ⏳م١ + م١٥ | [`05`](05-rbac-matrix.md) · [`08`](08-testing-strategy.md) §5 |
| ٤٥ | الأدوار السبعة | ✅ **م١** | مبذورةٌ ومُسندة، ورفضٌ افتراضيّ بخريطةِ مساراتٍ تُفحص في CI ([ADR-002](00-decisions.md)) |
| ٤٦ | سجلّ التدقيق بالقيمة قبل وبعد | ✅ **م١** | `audit_logs` — لا يُعدَّل ولا يُحذف (قاعدةُ `DO INSTEAD NOTHING`) |
| ٤٧ | ترتيب المعمار قبل الواجهات | ✅ **مُطبَّق** | هذه المرحلة كلُّها |
| ٤٨ | المحظورات الاثنا عشر | ✅ **مُطبَّقة** | أدناه |
| ٤٩ | التحليل قبل الكود | ✅ **مُطبَّق** | الوثائق العشر |
| ٥٠ | التنفيذ بمراحل | 📐 | [`07`](07-roadmap.md) — ١٦ مرحلة |
| ٥١ | **بوّابة عبورٍ لكل مرحلة** | 📐 | [`07`](07-roadmap.md) — **قابلةٌ للفحص لا للتقدير** |
| ٥٢ | الاختبارات ومنها التزاحم | 📐 | [`08`](08-testing-strategy.md) ✅ **مُختبَر: لا `-1`** |
| ٥٣ | الأداء والتوسّع | ⏳م١٥ | ترقيمٌ بالمؤشّر · فهارس · طوابير — [`04`](04-api-contract.md) §0 |
| ٥٤ | الجوال أولاً · RTL | ✅ **م٩** | [`06`](06-saudi-layer.md) §5 · بوّابةُ كل مرحلة |
| ٥٥ | معالجة الصور | ⏳م٢ | WebP بأحجامٍ متعدّدة — بوّابةُ م٢ |
| ٥٦ | معمارٌ جاهزٌ للتطبيقات | 📐 | [ADR-003](00-decisions.md) — **خادمٌ واحد، والتطبيقُ صفرُ سطرٍ فيه** |
| ٥٧ | تعدّد القنوات | 📐 | `sales_channels` · `api_keys` |
| ٥٨ | تاجرٌ واحد ثم سوق | 📐 | [ADR-004](00-decisions.md) |
| ٥٩ | **معمار السوق من البداية** | 📐 | `vendors` · `vendor_commissions` — **موجودةٌ ومعطَّلة** |
| ٦٠ | النتيجة: منصّةٌ لا متجر | 📐 | الوثائق العشر |

---

## بند ٤٨ — المحظورات الاثنا عشر وكيف مُنع كلٌّ منها

| المحظور | ما يمنعه هنا |
|---|---|
| نموذجٌ أوّليّ بسيط | المرحلة ٠ معمارٌ لا واجهة |
| واجهةٌ بلا خادم | [ADR-003](00-decisions.md): **صفرُ منطقٍ في الواجهة** |
| بياناتٌ وهمية في مسارٍ حقيقيّ | بوّابةُ كل مرحلة |
| **قواعدُ عملٍ مبرمَجة** | كلُّها صفوف: `prices` · `tax_rates` · `shipping_rates` · `promotion_rules` · `loyalty_settings` |
| أسعارٌ أو ضرائبُ أو شحنٌ في الكود | نفسه + **grep في CI** |
| تصميمُ القاعدة بعد الواجهة | المخطط أُنجز في المرحلة ٠ ✅ |
| أزرارٌ لا تفعل شيئاً | بوّابةُ كل مرحلة |
| ميزةٌ «مكتملة» بلا اختبار | البوّابةُ تشترط الاختبارات |
| حذفُ ميزةٍ لأنها صعبة | **هذا الجدول** — الستّون كلُّها لها حكم |
| بلا تدقيق | `audit_logs` ✅ **مُختبَر** |
| بلا ضبطٍ إداريّ | كلُّ قاعدةٍ صفٌّ يضبطه المدير |
| بلا تصميمٍ للتوسّع | تعدّدُ القنوات والمواقع والبائعين من اليوم |

---

## ما أُعدِّل من مواصفتك — بندان لا غير، وبحجّة

**١. بند ١١ — آلة الحالات.** عمودٌ واحد بستّ عشرة حالة **يخلط ثلاثة
محاور** ولا يستطيع وصف طلبٍ شُحن بعضُه واسترُدّ بعضُه. البرهانُ والحلُّ
وجدولُ التعبير في [`03`](03-state-machines.md) §0. **ولم تسقط حالة.**

**٢. بند ٤١ — الذكاء.** يُؤجَّل كلُّه إلى المرحلة ١٤ (وهو موضعُه في
ترتيبك) لأنه **دوالُّ في سجلٍّ تاريخيّ لا نملكه بعد**. ويبقى في المعمار
نقطةُ ربط.

**وما عدا هذين: مأخوذٌ كما كتبتَه.**
